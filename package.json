{
  "name": "sideproject-ai",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm turbo run build",
    "deploy:api": "pnpm turbo run deploy --filter=api --"
  },
  "author": "Ali Bassiouni",
  "description": "",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@types/node": "^20.11.5",
    "turbo": "^1.13.2",
    "typescript": "^5"
  }
}
